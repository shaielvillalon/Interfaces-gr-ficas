<Window x:Class="PracticaFinalInterfaces3.EliminarEjercicio"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PracticaFinalInterfaces3"
        mc:Ignorable="d"
        Title="EliminarEjercicio" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Margin="0,20" Content="Seleccione el ejercicio que desea eliminar: " HorizontalAlignment="Center" FontSize="25"/>


        <DataGrid x:Name="ListaEjerciciosEliminar" Margin="10,20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding Ejercicios}" 
              AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="ListaEjerciciosEliminar_SelectionChanged" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="3*" CanUserSort="False" />
                <DataGridTextColumn Header="Grupos Musculares" Binding="{Binding GruposMusculares}" Width="1.5*" CanUserSort="False"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="20" Grid.Row="2">
            <Button Name="BotonEliminarEjercicio" Click="BotonEliminarEjercicio_Click" IsEnabled="False" Content="Eliminar ejercicio" Width="125" Margin="5"/>
        </StackPanel>

    </Grid>
</Window>
